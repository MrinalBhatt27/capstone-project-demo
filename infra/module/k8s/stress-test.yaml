apiVersion: v1
kind: Pod
metadata:
  name: stress-test-pod
spec:
  containers:
  - name: stress-container
    image: polinux/stress
    command: ["stress"]
    args: ["--vm", "3", "--vm-bytes", "800M", "--timeout", "180s"]
    resources:
      limits:
        memory: "2500Mi"
        cpu: "1000m"